<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.hero.rogueb.world.mapper.ServiceMapper">
    <select id="findByType" resultMap="string">
        SELECT endpoint FROM Service WHERE type = #{type};
    </select>

</mapper>
